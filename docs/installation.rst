============
Installation
============

At the command line::

    $ easy_install django-tips

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-tips
    $ pip install django-tips
